/**
 * HomePage — Game-mode selection screen.
 *
 * Stub: full implementation in Step 5.
 * Routing and App shell are wired here so the build succeeds.
 */
export default function HomePage() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <p className="text-text-secondary font-mono">Loading homepage…</p>
    </div>
  )
}
