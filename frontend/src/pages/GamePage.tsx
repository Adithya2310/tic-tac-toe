/**
 * GamePage — Active game session screen.
 *
 * Stub: full implementation in Steps 6–11.
 * Routing and App shell are wired here so the build succeeds.
 */
export default function GamePage() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <p className="text-text-secondary font-mono">Loading game…</p>
    </div>
  )
}
